# Dependencias y artefactos (se instalan/generan dentro de la imagen)
node_modules/
dist/
.expo/
android/
ios/

# El .env SÍ entra al build: expo export lo lee y hornea las EXPO_PUBLIC_*
# en el bundle (no son secretos). Editar frontend/.env y reconstruir.

Dockerfile
.dockerignore
